name: Bootstrap source retired

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Bootstrap completed and this workflow is retired."
